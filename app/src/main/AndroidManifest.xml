<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hw_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="landscape"
        android:supportsRtl="true"

        android:theme="@style/Theme.HW_1">


        <activity
            android:name=".activities.Activity_Main">
        </activity>

        <activity
            android:name=".activities.Activity_Winner_List">
        </activity>

        <activity android:name=".activities.Activity_Winner">
        </activity>

        <activity android:name=".activities.Activity_welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCEpFR2c2Pe03psDndMAfDh0lPFCbwaTdc" />

    </application>

</manifest>